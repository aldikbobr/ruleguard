# Скопируйте в .env и заполните. Файл .env не попадает в git.
# Пока ни один ключ не нужен: все используемые API публичные.

# Для ИИ-разбора правил (следующий этап)
ANTHROPIC_API_KEY=

# Для продакшен-доступа к DFlow (Kalshi на Solana)
DFLOW_API_KEY=

# Copy to .env and fill in. The .env file is never committed.
# No key is needed yet: all market data comes from public APIs.

# For AI rule review (next step)
ANTHROPIC_API_KEY=

# For production access to DFlow (Kalshi markets on Solana)
DFLOW_API_KEY=
